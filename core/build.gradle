description = "The core components: plugin manager and gui"

dependencies {
  runtime (group: "org.neo4j.app", name: "neo4j-server", version: "1.8.M03", classifier: "static-web") {
    exclude module: "logback-classic"
    exclude module: "logback-access"
  }

  compile group: "com.sun.jersey", name: "jersey-core", version: "1.12"
  compile group: "com.sun.jersey", name: "jersey-server", version: "1.12"
}
